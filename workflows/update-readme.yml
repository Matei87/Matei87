name: Update README

on:
  schedule:
    - cron: '*0 * * * *'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    name: Update this repo's README with recent activity

    steps:
      - uses: actions/checkout@v2
      - uses: Matei87/github-activity-readme@master
        env:
          GITHUB_TOKEN: ${{ a8d78463e389a3adaba3e2c2bd499bbe3d8f4e8c }}
